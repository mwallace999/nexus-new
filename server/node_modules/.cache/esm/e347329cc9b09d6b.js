let express,http,Server,createBoard;_c9b‍.w("express",[["default",["express"],function(v){express=v}]]);_c9b‍.w("http",[["default",["http"],function(v){http=v}]]);_c9b‍.w("socket.io",[["Server",["Server"],function(v){Server=v}]]);_c9b‍.w("./controller/board",[["createBoard",["createBoard"],function(v){createBoard=v}]]);




const app = express();
const server = http.createServer(app);
const io = new Server(server);

// SOCKET.IO HANDLING
io.on('connection', (socket) => {
    console.log('A user connected');

    socket.on('disconnect', () => {
        console.log('A user disconnected');
    });

    socket.on('syncNewGame', (setup) => {
        const board = createBoard(setup);
        io.emit('newGame', board);
    });

    socket.on('syncBoardState', (boardState) => {
        console.log('SYNCING BOARD STATE');
        io.emit('boardState',  boardState);
    })
  
});

// START SERVER
const port = process.env.PORT || 3000;
server.listen(port, () => {
    console.log(`Server is running on port ${port}`);
});

